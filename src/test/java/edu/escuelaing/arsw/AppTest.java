/*package edu.escuelaing.arsw;

import java.io.FileNotFoundException;
import java.io.FileReader;
import java.io.IOException;

import edu.escuelaing.arsw.LinkedList.linkedlist;
import edu.escuelaing.arsw.calculator.Calculos;
import edu.escuelaing.arsw.calculator.Reader;
import junit.framework.Test;
import junit.framework.TestCase;
import junit.framework.TestSuite;



public class AppTest 
    extends TestCase
{
    
    public AppTest( String testName )
    {
        super( testName );
    }

   
    public static Test suite()
    {
        return new TestSuite( AppTest.class );
    }

    /**
     * Rigourous Test :-)
     */
    
    /*public void deberiaCalcularLaMedia() {
        Calculos calculos = new Calculos();
        linkedlist ln = new linkedlist();
        ln.agregar(3.4);
        ln.agregar(5.7);
        ln.agregar(9.0);
        Double res = calculos.calcularMedia(ln);
        assertEquals(6.03, res);
    }

    public void deberiaCalcularLaDesviacionEstandar() {
        Calculos calculos = new Calculos();
        linkedlist ln = new linkedlist();
        ln.agregar(3.4);
        ln.agregar(5.7);
        ln.agregar(9.0);
        Double res = calculos.calcularDesviacion(ln);
        assertEquals(2.81, res);
    }

    public void deberiaCalcularLaMediaLeyendoDeUnArchivo() throws IOException {
        FileReader fr = new FileReader("numeros.txt");
        Reader reader = new Reader();
        reader.readFile(fr);
        Double res = reader.getMedia();
        assertEquals(550.6, res);
    }

    public void deberiaCalcularLaDesviacionEstandarLeyendoDeUnArchivo() throws IOException {
        FileReader fr = new FileReader("numeros.txt");
        Reader reader = new Reader();
        reader.readFile(fr);
        Double res = reader.getDesviacionEstandar();
        assertEquals(572.03, res);
    }

    public void deberiaCalcularLaMediaLeyendoDeUnArchivo2() throws IOException {
        FileReader fr = new FileReader("numeros2.txt");
        Reader reader = new Reader();
        reader.readFile(fr);
        Double res = reader.getMedia();
        assertEquals(60.23, res);
    }

    public void deberiaCalcularLaDesviacionEstandarLeyendoDeUnArchivo2() throws IOException {
        FileReader fr = new FileReader("numeros2.txt");
        Reader reader = new Reader();
        reader.readFile(fr);
        Double res = reader.getDesviacionEstandar();
        assertEquals(62.24, res);
    }
}*/
